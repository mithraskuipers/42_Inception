cd /home/$USER/inception/srcs ;
docker-compose up -d ;

Browser -> localhost

It should show you something like:

403 Forbidden
nginx

Then clean up everything:
docker stop $(docker ps -aq) && docker kill $(docker ps -aq) && docker rm $(docker ps -aq) && docker rmi $(docker images -aq)
